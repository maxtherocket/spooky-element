# API Goals

```javascript
// Select an elements
new SpookyElement('.ghost', '#haunted-house');

// Renders the template
// ReallySpookyElement had a template assigned to it
var reallySpooky = new ReallySpookyElement({eyes:'angry', slimy:true}).appendTo( hauntedHouse );